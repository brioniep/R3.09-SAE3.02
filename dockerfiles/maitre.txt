FROM python:3.9-slim

WORKDIR /app

RUN apt-get update && apt-get install -y \
    gcc g++ openjdk-17-jdk && \
    apt-get clean && rm -rf /var/lib/apt/lists/*

COPY server.py .
COPY file-attente ./file-attente
COPY liste-envoie ./liste-envoie

CMD ["python3", "server.py"]
